% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simStatinData.R
\name{simStatinData}
\alias{simStatinData}
\title{Simulate Data in a Statin Setting}
\usage{
simStatinData(
  N,
  eta = rep(0.1, 6),
  nu = rep(1.1, 6),
  beta = beta,
  cens = 1,
  followup = 5,
  lower = 10^(-15),
  upper = 200,
  gen_A0 = NULL,
  ...
)
}
\arguments{
\item{N}{Numeric scalar. Number of individuals to simulate.}

\item{eta}{Numeric vector of length 6. Shape parameters for Weibull intensities with parameterization
\eqn{\eta \nu t^{\nu - 1}}. Defaults to \code{rep(0.1, 6)}.}

\item{nu}{Numeric vector of length 6. Scale parameters for the Weibull hazards. Defaults to \code{rep(1.1, 6)}.}

\item{beta}{Numeric matrix. Of dimension p times 6. Regression coefficients matrix where columns correspond to event types (N0, ..., N5) and rows correspond to covariates (L0, A0, L1, L2, ...) and event counts (N0, ..., N5). Default is a zero matrix.}

\item{cens}{Binary scalar. Indicates whether individuals are at risk of censoring (default \code{1}).}

\item{followup}{Numeric scalar. Maximum follow-up (censoring) time. Defaults to \code{Inf}.}

\item{lower}{Numeric scalar. Lower bound for root-finding (inverse cumulative hazard) (default \code{1e-15}).}

\item{upper}{Numeric scalar. Upper bound for root-finding (default 200).}

\item{gen_A0}{Function. Function to generate the baseline treatment covariate A0.
Takes N and L0 as inputs. Default is a Bernoulli(0.5) random variable.}

\item{...}{Additional arguments passed to \code{simEventData}}
}
\value{
A data frame containing the simulated data with columns:
\item{ID}{Individual identifier}
\item{Time}{Time of the event}
\item{Delta}{Event type (0,...,5)}
\item{L0}{Baseline covariate}
\item{A0}{Baseline covariate}
\item{L1,...Lp}{Additional baseline covariates}
}
\description{
Simulate Data in a Statin Setting
}
\examples{
simDisease(10)

}
